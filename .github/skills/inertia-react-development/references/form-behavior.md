# Form behavior

- Type every field and error.
- Use server validation as the authority.
- Keep field values after validation errors.
- Disable duplicate submission.
- Show field errors next to the control.
- Confirm destructive operations.
- Move long workflows to a page rather than an oversized dialog.
- After success, redirect to a stable route and show feedback.
